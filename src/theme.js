import { createMuiTheme } from '@material-ui/core/styles';

const s = createMuiTheme({
    palette: {
      primary: { main:'#FF3B3F' },
      secondary: {
        main: '#f44336',
      },
    }, 
}, 'watermelon' )

export default s